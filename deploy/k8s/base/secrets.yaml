# Template — replace values with base64-encoded secrets before applying.
# In production, use a secret manager (AWS Secrets Manager, Vault, etc.)
# and an operator like External Secrets to populate these.
apiVersion: v1
kind: Secret
metadata:
  name: de-platform-secrets
  namespace: de-platform
type: Opaque
data:
  # echo -n 'value' | base64
  DB_POSTGRES_PASSWORD: cGxhdGZvcm0=        # "platform"
  DB_CLICKHOUSE_PASSWORD: Y2xpY2tob3VzZQ==  # "clickhouse"
  FS_MINIO_ACCESS_KEY: bWluaW9hZG1pbg==     # "minioadmin"
  FS_MINIO_SECRET_KEY: bWluaW9hZG1pbg==     # "minioadmin"
  JWT_SECRET: ZGV2LWs4cy1qd3Qtc2VjcmV0LW1pbmltdW0tMzItYnl0ZXMhIQ==  # dev only — MUST be changed in production
